<!DOCTYPE html>

<title>Edit Imputation</title>

{% block body %}
    <h1>Edit Imputation</h1>

    {{ include('imputation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('imputation_index') }}">back to list</a>

    {{ include('imputation/_delete_form.html.twig') }}
{% endblock %}
