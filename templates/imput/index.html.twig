{% extends 'header.html.twig' %}
{% block body %}
	<!--{{ dump(datas) }}-->

	<!-- Content Wrapper. Contains page content -->
		<div
		class="content-wrapper"> <!-- Content Header (Page header) -->
		<div class="content-header">
			<div class="container-fluid">
				<div class="row mb-2">
					<div class="col-sm-6">
						<h1 class="m-0">
							Imputation
						</h1>
					</div>
				</div>
				<!-- /.row -->
			</div>
			<!-- /.container-fluid -->
		</div>
		<!-- /.content-header -->

		<!-- Main content -->
		<div class="col-lg-12">

			<div class="card card-primary card-outline cardT">
				<div class="card-header">
					<div class="rowT">
						<div class="col-sm-4">
							<div class="form-group">
								<select class="form-control select2" id="name" style="width: 100%;">

									{% for user in users %}
										<option value='{{ user.id }}'>{{ user.username }}</option>
									{% endfor %}
								</select>

							</div>
						</div>
						<div class="col-sm-3">

							<div class="input-group">
								<input type="week" value='<?=date("yyyy-MM")?>' id="date-input" class="form-control" name="month"/>
							</div>
						</div>
						<div class="col-sm-2">
							<button type="button" id="btnRech" class="btn btn-block btn-info">Rechercher</button>
						</div>
						<div class="col-sm-3">
							<div class="btn-group">
								<button type="button" class="btn btn-info btn-flat">Exporter vers</button>
								<button type="button" class="btn btn-info btn-flat dropdown-toggle dropdown-icon" data-toggle="dropdown">
									<span class="sr-only">Toggle Dropdown</span>
								</button>
								<div class="dropdown-menu" role="menu">
									<a class="dropdown-item" onclick="exportAll('csv');" href="javascript://">CSV</a>
									<a class="dropdown-item" onclick="exportAll('txt');" href="javascript://">TXT</a>
									<a class="dropdown-item" onclick="exportAll('xls');" href="javascript://">XLS</a>
									<a class="dropdown-item" onclick="exportAll('sql');" href="javascript://">SQL</a>
								</div>
							</div>
						</div>

					</div>

				</div>


				<div class="card">

					<div class="card-body p-0 table-responsive">
						<table class="table" id="firm_table">
							<thead id="entete"></thead>
							<tbody id="tableB"></tbody>
						</table>
					</div>


					<!--container-->
				</div>
				<a href="{{ path('imput_new') }}">Ajoutez une imputation</a>
			</div>
		</div>
		<!-- /.content -->
	</div><!-- /.content-wrapper --><!-- Main Footer -->{% endblock %}
