<form method="post" action="{{ path('imput_delete', {'id': imput.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ imput.id) }}">
    <button class="btn">Delete</button>
</form>
